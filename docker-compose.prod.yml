services:
  mongodb:
    image: mongo:latest
    restart: unless-stopped
    environment:
      MONGO_INITDB_ROOT_USERNAME: ${PROD_DB_USER}
      MONGO_INITDB_ROOT_PASSWORD: ${PROD_DB_PASSWORD}
    volumes:
      - mongo-prod-data:/data/db
    healthcheck:
      test: ["CMD", "mongosh", "--quiet", "--eval", "db.runCommand({ ping: 1 }).ok"]
      interval: 20s
      timeout: 10s
      retries: 10
      start_period: 20s
    networks:
      - prod-network

  rabbitmq:
    image: rabbitmq:3-management
    restart: unless-stopped
    ports:
      - "${RABBITMQ_PORT:-5672}:5672"
      - "${RABBITMQ_MANAGEMENT_PORT:-15672}:15672"
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "check_port_connectivity"]
      interval: 20s
      timeout: 10s
      retries: 10
      start_period: 20s
    networks:
      - prod-network

  api:
    build:
      context: ./api
      dockerfile: Dockerfile.prod
    restart: unless-stopped
    environment:
      - PORT=${API_PORT:-3005}
      - DB_NAME=${DB_NAME:-devops_opdrachten}
      - MONGO_URL=mongodb://${PROD_DB_USER}:${PROD_DB_PASSWORD}@mongodb:27017/${DB_NAME:-devops_opdrachten}?authSource=admin
      - RABBITMQ_URL=amqp://rabbitmq:5672
      - USER_CREATED_QUEUE=user.created
    depends_on:
      mongodb:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    expose:
      - "${API_PORT:-3005}"
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://127.0.0.1:' + (process.env.PORT || 3005) + '/', (r)=>process.exit(r.statusCode < 500 ? 0 : 1)).on('error', ()=>process.exit(1))"]
      interval: 20s
      timeout: 10s
      retries: 10
      start_period: 20s
    networks:
      - prod-network

  orders-mongodb:
    image: mongo:latest
    restart: unless-stopped
    volumes:
      - orders-mongo-prod-data:/data/db
    healthcheck:
      test: ["CMD", "mongosh", "--quiet", "--eval", "db.runCommand({ ping: 1 }).ok"]
      interval: 20s
      timeout: 10s
      retries: 10
      start_period: 20s
    networks:
      - prod-network

  orders-service:
    build:
      context: ./orders-service
      dockerfile: Dockerfile.prod
    restart: unless-stopped
    environment:
      - PORT=${ORDERS_PORT:-3010}
      - ORDERS_DB_NAME=${ORDERS_DB_NAME:-orders_db}
      - ORDERS_MONGO_URL=mongodb://orders-mongodb:27017
      - RABBITMQ_URL=amqp://rabbitmq:5672
      - USER_CREATED_QUEUE=user.created
    expose:
      - "${ORDERS_PORT:-3010}"
    depends_on:
      orders-mongodb:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://127.0.0.1:' + (process.env.PORT || 3010) + '/orders', (r)=>process.exit(r.statusCode < 500 ? 0 : 1)).on('error', ()=>process.exit(1))"]
      interval: 20s
      timeout: 10s
      retries: 10
      start_period: 20s
    networks:
      - prod-network

  prometheus:
    build:
      context: ./prometheus
      dockerfile: Dockerfile
    restart: unless-stopped
    volumes:
      - prometheus-prod-data:/prometheus
    ports:
      - "${PROMETHEUS_PORT:-9090}:9090"
    depends_on:
      api:
        condition: service_started
      orders-service:
        condition: service_started
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:9090/-/ready"]
      interval: 20s
      timeout: 10s
      retries: 10
      start_period: 15s
    networks:
      - prod-network

  mongodb-exporter:
    image: percona/mongodb_exporter:0.40.0
    restart: unless-stopped
    command:
      - --mongodb.uri=mongodb://${PROD_DB_USER}:${PROD_DB_PASSWORD}@mongodb:27017/admin?authSource=admin
      - --collect-all
      - --compatible-mode
    depends_on:
      mongodb:
        condition: service_healthy
    networks:
      - prod-network

  orders-mongodb-exporter:
    image: percona/mongodb_exporter:0.40.0
    restart: unless-stopped
    command:
      - --mongodb.uri=mongodb://orders-mongodb:27017/admin
      - --collect-all
      - --compatible-mode
    depends_on:
      orders-mongodb:
        condition: service_healthy
    networks:
      - prod-network

  grafana:
    build: ./grafana
    restart: unless-stopped
    ports:
      - "${GRAFANA_PORT:-3000}:3000"
    depends_on:
      prometheus:
        condition: service_started
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:3000/api/health"]
      interval: 20s
      timeout: 10s
      retries: 10
      start_period: 30s
    networks:
      - prod-network

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.prod
    restart: unless-stopped
    environment:
      - API_BASE_URL=${FRONTEND_API_BASE_URL:-/api}
    ports:
      - "${FRONTEND_PORT:-80}:80"
    depends_on:
      api:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1/"]
      interval: 20s
      timeout: 10s
      retries: 10
      start_period: 20s
    networks:
      - prod-network

volumes:
  mongo-prod-data:
  orders-mongo-prod-data:
  prometheus-prod-data:

networks:
  prod-network: